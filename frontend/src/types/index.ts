export interface Bachelor {
    not_id: string;
    not_title: string;
    not_date: string;
    not_url: string;
}

export interface Event {
    evt_id: string;
    evt_title: string;
    evt_date: string;
    evt_url: string;
}

export interface Cheering {
    cheer_id: number;
    cheer_message: string;
}
